1.堆内存不宜设置太小，实验中堆设置128M和256M的情况，会发生OOM导致系统异常；
堆设置的太小，不管哪一种GC策略都会频繁的发生youngGC和fullGC,导致大部分时间在GC.
2.不管哪一种GC策略,随着堆内存的增大，发生GC的频次就越低，fullGC的频次也越来越低，但是单次发生GC的时间会变长。
3.内存相同，串行GC时间会长于并行GC,并发GC和G1GC.
4.内存越大，G1回收效率越高，gc时间比其他类型短，吞吐量比其他类型高。内存越大cms回收的时间不太稳定。
5.内存不宜设置太大，内存大于4G~6G以后,单次垃圾回收（STW）的时间就会特别长,导致程序有卡顿现象。